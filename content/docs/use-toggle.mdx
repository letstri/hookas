---
title: Use Toggle
description: Toggle a value.
---

#### Installation

```bash
npx shadcn@latest add https://hookas.letstri.dev/r/use-toggle.json
```

#### Demo

import { ToggleDemo } from '@/components/demo/use-toggle'

<ToggleDemo />

#### Usage

```tsx
import { useToggle } from '@/hookas/use-toggle'

function ToggleButton() {
  const [isOn, toggle] = useToggle(false)

  return (
    <div>
      <button onClick={toggle}>{isOn ? 'ON' : 'OFF'}</button>
      <span>
        State:
        {isOn ? 'Enabled' : 'Disabled'}
      </span>
    </div>
  )
}
```

[Source](https://github.com/letstri/hookas/blob/main/registry/hooks/use-toggle.ts)
